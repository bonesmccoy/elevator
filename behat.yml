default:
    suites:
        default:
            contexts:
                - ResidentContext

    extensions:
        RMiller\BehatSpec\BehatExtension:
            path: bin/phpspec
            config: phpspec.yml